import { Box } from '@mui/material'

const Root = () => {
    return (
        <Box sx={{ height: '100%', width: '100%', display: 'flex', flexDirection: 'column' }}>
            Root
        </Box>
    )
}

export default Root